name: Conciliar Catalogo MercadoLibre Tienda 2
on:
  schedule:
    # Lunes a Sábado a las 6:15 PM local (Martes a Domingo a las 00:15 UTC)
    - cron: '15 0 * * 0,2-6'
  workflow_dispatch: # Botón manual en GitHub

jobs:
  run-conciliation:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          
      - name: Instalar Dependencias
        run: pip install -r requirements.txt
        
      - name: Ejecutar Conciliador
        env:
          SPREADSHEET_ID: ${{ secrets.SPREADSHEET_ID_TIENDA2 }}
          GOOGLE_SERVICE_ACCOUNT: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_TIENDA2 }}
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL_TIENDA2 }}
        run: python conciliador_publicaciones.py
